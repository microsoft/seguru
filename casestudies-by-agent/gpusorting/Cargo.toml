[package]
name = "gpusorting-by-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
gpu = { workspace = true }
gpu_host = { workspace = true }

[dev-dependencies]
rand = "0.9"

[features]
bench = []

[[bin]]
name = "sort-bench"
path = "src/bin/bench.rs"
required-features = ["bench"]
