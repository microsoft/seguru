[package]
name = "gpu"
version = "0.1.0"
edition = "2021"

[dependencies]
cuda_bindings = { workspace = true, features = ["gpu"]}
num-traits = { workspace = true }

[features]
default = []
codegen_tests = []