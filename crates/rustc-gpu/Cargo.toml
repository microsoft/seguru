[package]
name = "rustc-gpu"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3" }
mlir-compile = {path = "../mlir-compile"}

# To embed librustc_codegen_gpu.so bytes
rustc_codegen_gpu = {path = "../rustc_codegen_gpu"}