[workspace]
members = [
    "hello",
    "llm-rs-gpu",
    "matrix/matmul-gpu",
    "matrix/matmul-host",
    "syntax/gpu",
    "syntax/host",
]
resolver = "1"

[workspace.dependencies]
gpu_host = {path = "../crates/gpu-host"}
gpu_macros = { path = "../crates/gpu-macros" }
gpu = { path = "../crates/gpu" }
syntax_gpu = {path = "syntax/gpu"}
