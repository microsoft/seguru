[workspace]
members = [
    "manual-test-gpu-arith",
    "manual-test-host-arith",
    "llm-rs-gpu",
    "matrix/matmul-gpu",
    "matrix/matmul-host",
]

[workspace.dependencies]
gpu_host = {path = "../crates/gpu-host"}
gpu_macros = { path = "../crates/gpu-macros" }
gpu = { path = "../crates/gpu" }
