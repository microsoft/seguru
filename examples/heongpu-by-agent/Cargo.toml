[package]
name = "heongpu-gpu"
version = "0.1.0"
edition = "2024"

[dependencies]
gpu = { workspace = true }
gpu_host = { workspace = true }

[dev-dependencies]
rand = "0.9"

[features]
bench = []

[[bin]]
name = "heongpu-bench"
path = "src/bin/bench.rs"
required-features = ["bench"]
