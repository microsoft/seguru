[package]
name = "llm-rs-gpu"
version = "0.1.0"
edition = "2021"

[dependencies]
gpu = {workspace = true}
gpu_macros = {workspace = true}
gpu_host = {workspace = true}
crunchy = "0.2"

[dev-dependencies]
rand = "0.9"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"