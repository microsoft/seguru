[package]
name = "manual-test-host-arith"
version = "0.1.0"
edition = "2021"

[dependencies]
cuda_bindings = {workspace = true}
manual-test-gpu-arith = {path = "../manual-test-gpu-arith"}
gpu_macros = {workspace = true}
gpu = {workspace = true}
