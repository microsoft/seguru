[package]
name = "matmul-gpu"
version = "0.1.0"
edition = "2021"

[dependencies]
gpu = {workspace = true, features = ["unstable_host_chunk"]}
gpu_host = {workspace = true}

[features]
default = ["v1"]
v1 = []
v2 = []
v3 = []