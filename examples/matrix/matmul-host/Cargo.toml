[package]
name = "matmul-host"
version = "0.1.0"
edition = "2021"

[dependencies]
cuda_bindings = {workspace = true}
matmul-gpu = {path = "../matmul-gpu"}
gpu_macros = {workspace = true}
gpu = {workspace = true}