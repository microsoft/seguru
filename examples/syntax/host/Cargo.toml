[package]
name = "syntax_host"
version = "0.1.0"
edition = "2021"

[dependencies]
gpu_host = {workspace = true}
syntax_gpu = {workspace = true}
gpu_macros = {workspace = true}
gpu = {workspace = true}
