[workspace]
members = [
    "matmul-host",
    "matmul-gpu",
]

[workspace.dependencies]
cuda_bindings = {path = "../crates/cuda_bindings"}
gpu_macros = { path = "../crates/gpu-macros" }
gpu = { path = "../crates/gpu" }
